<div id="<%= dom_id word %>" class="flex gap-5">
  <p class="my-5">
    <strong class="block font-medium mb-1 text-2xl font-black">Word: <%= word.word %></strong>

  </p>

  <% if action_name != "show" %>
    <%= link_to "Show this word", word, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to 'Edit this word', edit_word_path(word), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
